name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-and-validate:
    if: ${{ !github.event.repository.is_template }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Lint Markdown
        uses: DavidAnson/markdownlint-cli2-action@05f32b88b2509cb3e4964b59a513b99ba82c9c0c # v19.1.0
        with:
          globs: "**/*.md"
          config: .markdownlint.yaml

      - name: Validate JSON
        run: |
          set -euo pipefail
          find .claude -name "*.json" | while read -r f; do
            python3 -m json.tool "$f" > /dev/null && echo "✅ $f" || { echo "❌ $f"; exit 1; }
          done

      - name: Validate structure
        run: bash .claude/scripts/validate-structure.sh
